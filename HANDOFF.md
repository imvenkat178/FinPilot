# FinPilot handoff

> Shared working memory for everyone (human or AI) who works on this repository.
> The protocol for using this file is in [AGENTS.md](AGENTS.md).
> Validate your edits with `python scripts/check_handoff.py --strict`.
>
> Sections 1 to 7 describe the **current** truth and are rewritten in place.
> Section 8 is an **append-only** log, newest entry first.

## 1. Last step (read this first)

- **When:** 2026-09-20
- **Who:** Claude Opus 5 (Claude Code)
- **What happened:** `G1.2` (client addresses behind a proxy) is implemented apart from its hosted confirmation. `scripts/verify_proxy_client_addresses.py` reproduces the lockout with an `nginx:alpine` container in front of a local server and writes `validation/proxy-client-addresses.json`; `FORWARDED_ALLOW_IPS` now has a documented default in the `Dockerfile`, a pass-through in `compose.yaml`, entries in `.env.example` and `docs/configuration.md` and a section in `DEPLOYMENT.md`; `warn_about_proxy_trust` in `finpilot/api/app.py` warns in production while only loopback is trusted; `tests/test_proxy_client_addresses.py` covers both peers. `G1.2.1` to `G1.2.3` are done and `G1.2.4` waits for `G4.1`. Earlier in the session the owner asked to run FinPilot with Plaid and link a bank, then to fix what that turned up. `G8.1.6` and `G8.1.7` are done: `PlaidClient._post` in `finpilot/integrations/plaid.py` logs failed Plaid calls with environment, endpoint, status, error type, code and request ID (never the message, keys or tokens), and `scripts/verify_plaid_sandbox.py` imports `create_app` inside `run`, with two new tests in `tests/test_plaid_sandbox_check.py` and notes in `DEPLOYMENT.md` and `CHANGELOG.md`. Earlier in the session: The owner's saved keys, including those from a new Plaid account, work only on Plaid Sandbox; `production.plaid.com` returns `INVALID_API_KEYS` (`G8.2`). The owner linked Tartan Bank through the real Plaid Link window in Chrome with `user_good`; FinPilot imported 8 accounts and synced 365 transactions (`G8.1.5`). Found and recorded `G8.1.6` (Plaid error codes are not logged) and `G8.1.7` (importing `scripts/verify_plaid_sandbox.py` builds the app's database before a caller can set `FINPILOT_DATABASE_URL`). Because of `G8.1.7`, this session's Sandbox test user and Tartan Bank connection landed in `.local/finpilot.db` rather than the intended `.local/finpilot-sandbox.db`; the owner's git-ignored launcher `.local/run-plaid-production.py` is fixed and its `--check` flag prints `finpilot-sandbox.db` with `--sandbox` and `finpilot-real.db` without it.
- **State left behind (2026-09-20):** A server started by `.local/run-plaid-production.py --sandbox` may still be listening on 127.0.0.1:8100; it was started before the launcher fix and uses `.local/finpilot.db`. `.local/plaid-production.env` (git-ignored) holds the owner's current Plaid client ID and secret, which are Sandbox keys despite the file name, plus a generated `FINPILOT_TOKEN_KEY`; never print or commit it. `.local/plaid-sandbox.env` has no `FINPILOT_TOKEN_KEY`, so a server started from it alone reports bank linking as not enabled.
- **Earlier session (2026-09-15):** The owner asked Claude Code to implement six AI requirements: deterministic math with the model only wording answers, guardrails and grounding against advice hallucinations and out-of-domain prompts, semantic caching, a deterministic rule engine, output validation against source records, and confidence and citation scoring with clickable links. Model wording now carries figure tokens (`finpilot/ai/facts.py`) that the server fills from the deterministic reference answer, with added-advice and topic checks in `finpilot/ai/guardrails.py`. Answers link their records and carry schema-validated figure sources and evidence confidence (`finpilot/ai/evidence.py`), and verified wording and plans are cached in process (`finpilot/ai/wording_cache.py`). `G7.7`, `G7.9`, `G7.10` and `G7.3.3` are done. Measured on local Ollama with `scripts/measure_figure_tokens.py`: `llama3.2:latest` at 12 s kept model wording on 5 of 10 questions, 2 rejected (allowance wording 1, typed figure 1), 2 over budget or failed and 1 needed no model call; repeats came from the cache 5 of 5 times with 0 model calls; `llama3.1:8b-instruct-q4_K_M` at 12 s kept model wording on 3 of 10 questions, 0 rejected, 6 over budget or failed and 1 needed no model call; repeats came from the cache 3 of 3 times with 0 model calls (`AI_VALIDATION.md`). The Plaid work from 2026-09-14 and the Sandbox runs are still uncommitted as well.
- **State left behind:** Uncommitted: the files listed in section 2. `.local/plaid-sandbox.env` is git-ignored and holds the owner's Sandbox keys; never print or commit it. `prototype/` is still untracked.
- **Resume by:**
  1. Use `.venv/Scripts/python.exe` for repository scripts, and never print `.local/plaid-sandbox.env` or `.local/plaid-production.env`. Start the Plaid server with `.venv/Scripts/python.exe .local/run-plaid-production.py --sandbox`.
  2. Restart any FinPilot server started before 2026-09-19 so it logs Plaid error codes.
  3. Ask the owner whether to commit this work, and about `G8.9` and `G7.2`.
  4. For `G7.2`, run `scripts/measure_figure_tokens.py` against each candidate host (`G7.2.4`).
  5. Start `G1.2`.

## 2. Repository state

| Item | Current value |
| --- | --- |
| Branch | `main`, pushed to `origin/main` (https://github.com/imvenkat178/FinPilot.git). The remote also has `codex/conversational-finance` (last commit `bf7ab17`, 2026-09-12), which predates the memory system |
| Last commit | `dbbfa79` (2026-09-14) "feat: live roadmap page with the current focus and a republish hook", pushed to `origin/main` |
| Uncommitted | New: `finpilot/ai/evidence.py`, `finpilot/ai/facts.py`, `finpilot/ai/wording_cache.py`, `finpilot/api/request_logging.py`, `finpilot/integrations/categories.py`, `scripts/measure_figure_tokens.py`, `scripts/verify_plaid_sandbox.py`, `tests/test_aggregation_categories.py`, `tests/test_answer_guards.py`, `tests/test_bank_aggregation.py`, `tests/test_evidence_links.py`, `tests/test_fact_tokens.py`, `tests/test_plaid_sandbox_check.py`, `tests/test_request_logging.py`, `tests/test_wording_cache.py`, `validation/figure-tokens-llama3.1-8b-12s.json`, `validation/figure-tokens-llama3.1-8b-60s.json`, `validation/figure-tokens-llama3.2-12s.json`, `validation/figure-tokens-llama3.2-60s.json`, `validation/plaid-sandbox-e2e.json`. Changed: `AGENTS.md`, `AI_VALIDATION.md`, `ARCHITECTURE.md`, `CHANGELOG.md`, `DEPLOYMENT.md`, `Dockerfile`, `HANDOFF.md`, `README.md`, `ROADMAP.md`, `docs/api-reference.md`, `docs/api/openapi.json`, `docs/calculation-methods.md`, `docs/configuration.md`, `docs/roadmap.html`, `docs/user-guide.md`, `finpilot/ai/graph.py`, `finpilot/ai/guardrails.py`, `finpilot/ai/mock_server.py`, `finpilot/ai/tools.py`, `finpilot/ai/workflows.py`, `finpilot/api/app.py`, `finpilot/api/assistant_routes.py`, `finpilot/api/bank_routes.py`, `finpilot/api/document_routes.py`, `finpilot/api/finance_routes.py`, `finpilot/api/route_docs.py`, `finpilot/api/workspace.py`, `finpilot/api/workspace_routes.py`, `finpilot/engine/ledger.py`, `finpilot/integrations/plaid.py`, `finpilot/models.py`, `finpilot/runtime.py`, `finpilot/services/bank_operations.py`, `finpilot/services/workspace.py`, `finpilot/web/accounts.js`, `finpilot/web/app.js`, `finpilot/web/assistant.css`, `finpilot/web/assistant.js`, `finpilot/web/connect.js`, `finpilot/web/manage.js`, `finpilot/web/pages.js`, `tests/assistant_frontend.mjs`, `tests/frontend.mjs`, `tests/management_frontend.mjs`, `tests/test_bank_linking.py`, `tests/test_capability_workflows.py`, `tests/test_documents.py`, `tests/test_guardrail_numeric_formats.py`, `tests/test_hosted.py`, `tests/test_llm_runtime.py`, `tests/test_runtime_performance.py`, `tests/test_spending_claim_guard.py`, `tests/test_workspace_ui.py`. `prototype/` stays untracked |
| What `76f95b1` contains | The reviewed AI workflow layer (80 typed capabilities, preview and confirm, receipts), saved conversations, document library with citations, MCP client and server, sample payment actions, their validation evidence, and the handoff system itself (`AGENTS.md`, `CLAUDE.md`, `HANDOFF.md`, `scripts/check_handoff.py`, `tests/test_handoff.py`, `.github/PULL_REQUEST_TEMPLATE.md`). Described in `END_TO_END_VALIDATION.md` and `ARCHITECTURE.md` |
| Toolchain verified here | Python 3.12.14 in `.venv/`, Node v24.19.0, Windows 11 |
| Last full test run | 2026-09-20: `pytest -q -p no:cacheprovider --basetemp <scratch>` gave 841 passed and 3 failed in 273.10s; the 3 were `tests/test_handoff.py` and `tests/test_roadmap.py` reading `HANDOFF.md` and `ROADMAP.md` while they were being edited, and all 15 passed afterwards. Frontend suites not run (no web change). Before that, 2026-09-15: `pytest -q -p no:cacheprovider` gave 832 passed, 1 warning in 170.31s after the figure-token, evidence and cache changes. All seven frontend suites passed the same day, with `tests/frontend.mjs` against a fresh local server. Timings on this machine swing with background load |
| CI | None on GitHub; the workflow is blocked on the token's `workflow` scope (`G5.8`). Locally, `.githooks/pre-push` runs the memory checks where `core.hooksPath` is set |
| Local model used for AI evidence | Ollama. The workflow matrix used `llama3.2:latest` (3.2B Q4_K_M, CPU) at a 12 s budget. Figure-token wording on 2026-09-15 used `llama3.2:latest` and `llama3.1:8b-instruct-q4_K_M` at 12 s and 60 s budgets (`AI_VALIDATION.md`) |

## 3. Done (capability ledger)

Working and covered by tests unless noted. Details live in `README.md` ("Available workflows") and the validation reports.

- **Identity and tenancy:** sign up, sign in, revocable sessions, household boundary derived from the session on every route.
- **Finance core:** accounts, CSV import with mapping and dedupe, income, bills and recurrences, paycheck allocation, goals, rules, cash-flow projection, debt and card analysis, tax and protection calculators.
- **Persistence:** versioned allowlisted JSON snapshots with `Decimal` amounts, PostgreSQL row locks and revision checks, Alembic migrations 0001 to 0008 (upgrade, repeat, downgrade, re-upgrade verified on SQLite and a local disposable PostgreSQL).
- **AI assistant:** deterministic router plus bounded capability planner, 80 typed workflow controls, compound reads, preview then explicit confirmation for every write, receipts restored from conversations, calculator-wording fallback, numeric grounding guards, injection checks on documents and MCP content, figure tokens so model wording never types a number, added-advice and topic checks, record links with figure sources and evidence confidence validated by JSON Schema, and an in-process cache of verified wording and plans.
- **Conversations and documents:** server-side saved conversations with URLs, PDF/TXT/Markdown library (2 MB, selectable text only), lexical retrieval with clickable citations.
- **MCP:** read-only MCP server with scoped expiring tokens; MCP client that imports approved source data as documents.
- **Bank linking:** Plaid Link for cash, credit card and loan accounts with encrypted tokens, cursor sync and disconnect; card and loan terms from Plaid Liabilities without guessed values, bank categories mapped to FinPilot categories, and CSV import refused for synced accounts. Mocked tests pass, and `scripts/verify_plaid_sandbox.py` passed against Plaid Sandbox on 2026-09-14 local time with evidence in `validation/plaid-sandbox-e2e.json`.
- **Sample payment simulation:** preflight, independent legs, idempotency, recovery, audit; restricted to sample workspaces.
- **Deployment assets:** nonroot Dockerfile, `compose.yaml`, `DEPLOYMENT.md`. Docker image not built or run here, though an `nginx:alpine` container ran in front of a local server for the proxy check (`G1.2`).
- **Private request logs:** one `finpilot.requests` line per request under any Uvicorn setup, with no query strings, bodies or amounts. Amounts and search text travel in JSON request bodies. `tests/test_request_logging.py` runs the container's Uvicorn command and plain Uvicorn locally.
- **Evidence:** benchmark scripts and dated JSON/text evidence under `validation/`; Llama interpretation matrix 115/140 exact, 77/80 capabilities with an accepted interpretation.
- **Agent coordination:** `AGENTS.md` protocol, `HANDOFF.md` validated by `scripts/check_handoff.py`, and `ROADMAP.md` validated and summarized by `scripts/roadmap.py`; both validators run inside pytest. A portable, tested copy for other repositories lives in `docs/agent-memory-kit/`. A branch check (`--base`) and a GitHub Actions workflow template exist; the workflow is not active yet (`G5.8`). Generated pages `docs/roadmap.html` and `docs/agent-memory-kit/guide.html` are checked for staleness, and `.githooks/pre-push` runs the memory checks before pushes. `scripts/roadmap_artifact.py` builds the owner's private roadmap page with a "Working on now" panel from this file, and a local Stop hook asks Claude Code sessions on the owner's machine to republish it whenever `ROADMAP.md` or this file changes.
- **Documentation:** a user guide, calculation methods, a configuration reference and frontend notes, plus generated API, capability and data model references in `docs/`, kept current by `tests/test_docs.py`.

## 4. Next (current focus)

The complete goal tree lives in [ROADMAP.md](ROADMAP.md): goals, sub-goals and tasks with horizon, priority, status and dependencies. Run `python scripts/roadmap.py next` for actionable work and pending decisions. Every roadmap ID below must exist and still be open; `scripts/check_handoff.py` enforces this.

Current focus, chosen by the last session:

1. `G1.2.4` Confirm on the hosted ingress that throttles key on the real client address, after `G4.1`. The reproduction, settings, docs and tests are done.
2. `G5.1` Continuous integration; the suite is fast enough that it no longer waits for `G5.2`.
3. Ask the owner to grant the `workflow` scope for `G5.8`, and to decide `G7.2` (production model host), `G2.1` (email provider) and `G6.2` (prototype directory).
4. Repeat the Plaid Sandbox journey against hosted staging after `G4.1` exists (`G8.1.4`). Ask whether coverage justifies a second aggregator such as MX or Yodlee (`G8.9`).
5. Documentation decisions for the owner: `G13.2` (license), `G13.3` (security reporting) and `G13.5` (specification).

## 5. Improvement areas (ideas, not commitments)

Moved into `ROADMAP.md` on 2026-09-13: test tiers (`G5.2`), one-command frontend verification (`G5.3`), planner latency (`G7.3`), evidence regeneration (`G5.7`), ledger split (`G10.1`) and shared inference admission (`G4.7`). Record new ideas in the "Ideas not yet goals" section of `ROADMAP.md`.

## 6. Known limits and risks (do not re-discover)

- Hosted multi-user load, TLS and network latency, live bank behaviour and external MCP OAuth are **unverified**. PostgreSQL checks were local and disposable.
- Real money movement is **not implemented** and must stay that way without an explicit product decision. Simulation is sample-workspace only.
- The Llama evidence is CPU-bound and shared the machine with other processes; treat timings as observations, not benchmarks.
- Only `tests/frontend.mjs` calls a live server and registers a throwaway user; it needs port 8100 or `FINPILOT_TEST_URL`. The other `tests/*_frontend.mjs` suites reference no server (checked 2026-09-14).
- `.local/` holds earlier evaluation runs that are deliberately not merged into the current matrix. Do not cite them as current.
- Web asset fingerprints are set at process start; a stale server serves stale JS.
- The application does not load `.env` on native startup; set variables in the shell or use compose.
- Proxy, load balancer and hosting platform request logs are outside FinPilot's control. `DEPLOYMENT.md` "Request logs" shows an nginx format without query strings; check the real ingress under `G4.1.5`. Since 2026-09-14 URLs carry no amounts or search text, so an unconfigured proxy records only identifiers, dates, counts and flags.
- A FinPilot server started before the 2026-09-14 request logging change serves the old `GET` calculator and search endpoints until it restarts. External scripts that call those `GET` URLs now get 404 or 405.
- uvicorn 0.52.4 trusts `X-Forwarded-For` only from `127.0.0.1` unless `FORWARDED_ALLOW_IPS` is set, so behind an ingress that is not on loopback the throttles in `finpilot/api/auth_routes.py` (register 8 per 5 min, login 30 per 5 min) key on the proxy address for every user. Measured with an nginx container on 2026-09-20 (`validation/proxy-client-addresses.json`). The operator must now set `FORWARDED_ALLOW_IPS`; `compose.yaml`, the `Dockerfile`, `.env.example`, `docs/configuration.md` and `DEPLOYMENT.md` say so, and production logs a warning while only loopback is trusted. Unverified on a real hosted ingress (`G1.2.4`).
- There is no background worker. Bank sync, rule runs and payment simulation only happen on a user request. Nothing sends reminders or notifications, and there is no email capability. Tracked as `G9.1`, `G9.2` and `G2.1`.
- Identity has no recovery or second factor: no password reset, email verification, MFA, password change or session management. Sessions last a fixed 12 hours. Tracked as `G2.2` to `G2.7`.
- Users cannot delete their account or export their data in the app. The MCP read bridge offers read access, not a download. Tracked as `G2.4` and `G2.5`.
- Totals skip accounts whose currency differs from the household base currency, and bank linking is US only. Tracked as `G10.3`.
- Plaid Link still requires Transactions, so an institution that offers only Liabilities, such as some loan servicers, does not appear in Link. `optional_products`, the Liabilities fields and the category labels passed against Plaid Sandbox institution `ins_109508` on 2026-09-14 local time, but production institutions and hosted staging remain unverified. Tracked as `G8.1`.
- A linked mortgage stays out of payoff and mortgage models until someone enters principal and interest and escrow separately, because Plaid reports one monthly payment that can include escrow. Auto and personal loans never get terms from Plaid.
- In the Claude Code sandbox on this machine, pytest cannot create its default temp or cache directories. Add `-p no:cacheprovider --basetemp <writable dir>`; verified 2026-09-13 with `tests/test_roadmap.py`.
- The GitHub CLI token on this machine has the scopes `gist`, `read:org` and `repo` but not `workflow`, so a push that adds files under `.github/workflows/` would be rejected. Checked with `gh auth status` on 2026-09-13; a push was not attempted. Tracked as `G5.8`.
- The generated pages load fonts from Google Fonts, and the guide page loads its Markdown renderer from cdnjs, so offline the guide shows raw Markdown. GitHub displays both files as source; open them in a browser, or enable GitHub Pages, to see them rendered.
- `core.hooksPath` is a per-clone setting, so the pre-push check only runs in clones where someone enabled it.
- Only Claude Code sessions on the owner's machine republish the owner's roadmap page: the Stop hook lives in the untracked `.claude/settings.local.json`, and only the owner can update that private page. Changes from other people or agents appear after the next such session stops. A hook file added during a session loads only after `/hooks` is opened or the session restarts.
- The repository is public, so `ROADMAP.md` and this file publicly describe unfixed weaknesses such as `G1.1` and `G1.2`. No hosted deployment exists yet.
- Remaining documentation gaps are tracked in `G13`: no `LICENSE`, no enabled private vulnerability reporting or promised response time, no specification for the requirement IDs cited in code, and no link from the app to `docs/user-guide.md`.
- Long answers still exceed local model limits. On 2026-09-15 `llama3.2:latest` fell back to calculator wording at the 12-second budget for: How should I split my next paycheck?; Compare debt payoff strategies with 200 extra monthly. At 60 seconds: How should I split my next paycheck? (model call failed: The local model did not finish its answer.). Rejected or over-budget answers use calculator wording (`AI_VALIDATION.md`).
- The topic gate (`in_domain` in `finpilot/ai/guardrails.py`) and the added-advice check are word lists. An unrelated request that uses a finance word reaches the normal deterministic path, a finance request with none of the listed words gets the fixed reply, and advice phrased outside the list is not caught. Add words when a real request is misjudged.
- The wording and plan cache lives in each process's memory, so separate workers do not share it and a restart empties it. Sharing it is an idea in `ROADMAP.md`.
- Figure tokens make every figure exact but do not prove that a sentence gives each figure its correct role. A 2026-09-15 draft from `llama3.2:latest` called interest paid 'saves [F5] interest' and fell back only because it also typed a number word. Only the spending allowance has a role check (`spending_allowance_claim_error`).
- Record links cover records named in calculation results, up to 20 per answer. Coverage, tax and product-boundary answers link no records.
- A failed Plaid call shows users "The bank connection could not be updated"; since 2026-09-19 the server log has a `finpilot.integrations.plaid` warning with the Plaid `error_code` (`DEPLOYMENT.md`). A server started before that change logs only the route and a 502. Bank linking also reports "not enabled" when `FINPILOT_TOKEN_KEY` is missing or not a valid Fernet key, even with Plaid keys set.

## 7. Decisions (do not re-litigate without a reason)

| Date | Decision | Why |
| --- | --- | --- |
| 2026-09-13 | Handoff lives in `HANDOFF.md` with the protocol in `AGENTS.md`; `CLAUDE.md` only imports `AGENTS.md` | One file every agent framework reads; one place to update |
| 2026-09-13 | Validator (`scripts/check_handoff.py`) runs inside pytest as `tests/test_handoff.py` | Any agent that runs the suite is reminded automatically |
| 2026-09-13 | Session log is append-only; archive to `docs/handoff-archive/` past about 30 entries | Keep the audit trail without bloating the file |
| 2026-09-13 | `prototype/` is excluded from commits (nested `.git`, not a submodule) until someone decides how to bring it in | `git add -A` would otherwise write a broken gitlink pointing at an untracked repo |
| 2026-09-11 | Per-operation household snapshot and SQLAlchemy session; no cross-request caching of financial answers except by household revision | Correctness under concurrency; see `ARCHITECTURE.md` |
| 2026-09-12 | Model output never executes; every write goes through preview and explicit confirmation | Financial safety; see `ARCHITECTURE.md` "Reviewed AI workflows" |
| 2026-09-13 | Long-term goals live in `ROADMAP.md` as goal, sub-goal and task IDs with horizon, priority, status and dependencies; `scripts/roadmap.py write` generates its summary; section 4 here lists only the current focus by ID | One goal tree that every agent reads; generated rollups cannot drift; the handoff stays short |
| 2026-09-13 | Suggestions from the production-readiness list the user supplied were merged into `ROADMAP.md`; ones FinPilot already had are marked done, and seven that conflict with existing design are under "Considered and not adopted" | Keeps revocable server sessions, verified-only answers and revision-keyed caching unless the user decides otherwise |
| 2026-09-13 | The memory system ships as a portable kit in `docs/agent-memory-kit/`; its script, test and PR-template copies must stay identical to this repository's, enforced by `tests/test_agent_memory_kit.py`, and repository-specific settings live in `.agent-memory.json` | Other repositories get exactly the tested system, and FinPilot cannot drift from it |
| 2026-09-13 | FinPilot is the reference installation of the agent memory kit: the `AGENTS.md` protocol text, the `ROADMAP.md` rules header and `CLAUDE.md` must match the kit templates exactly, enforced by `tests/test_agent_memory_kit.py` | The same exact instructions run here and in every repository that installs the kit |
| 2026-09-13 | The owner asked to commit and push the shared memory system; `main` was pushed to `origin` and `G6.1` is done | Agents that work from GitHub get the protocol, roadmap and kit |
| 2026-09-13 | CI enforces handoff updates with `check_handoff.py --base` on pushes and pull requests, not `--strict` | `--strict` depends on uncommitted files and commit dates that CI does not have; it stays the local pre-finish check |
| 2026-09-14 | The two shared pages are committed as generated files and checked for staleness, never edited by hand | The owner wanted them in the repository, and generation keeps them identical to their sources |
| 2026-09-14 | Until the workflow can be pushed, a versioned pre-push hook runs the same checks locally | It needs no GitHub permission and uses the same commands as the workflow |
| 2026-09-14 | Reference docs that mirror code are generated by `scripts/build_docs.py` and checked by `tests/test_docs.py`; endpoint text lives in `finpilot/api/route_docs.py` | Generated references cannot drift, and new routes or settings fail tests until they are documented |
| 2026-09-14 | `G5.1` no longer depends on `G5.2`, and `G5.2` is P2 | The full suite took 79 seconds on 2026-09-14, so CI does not need a fast tier first |
| 2026-09-14 | The owner's private roadmap page is built by `scripts/roadmap_artifact.py` from `ROADMAP.md` and `HANDOFF.md`, and a Stop hook in the owner's untracked `.claude/settings.local.json` enforces republishing; the page URL is not committed | Only the owner can publish to that page, so a committed hook would stop every other contributor's sessions, and the repository is public |
| 2026-09-14 | Amounts and free-text search travel in JSON request bodies: `POST` replaced `GET` for the six calculator reads and for transaction and document search, and `tests/test_request_logging.py` allowlists query parameters | Proxy, platform and error tracker logs record URLs, and FinPilot cannot configure them |
| 2026-09-14 | FinPilot adds its own log handler only when the host configured none, and filters query strings out of the Uvicorn access log instead of replacing Uvicorn's logging configuration | The request line then works under the container command, plain Uvicorn and operator logging configurations alike |
| 2026-09-14 | Card and loan terms come only from the aggregator or the user. A linked debt without a reported rate or required payment counts in totals but not in payoff models (`terms_complete`), and a card record needs a reported limit, statement date, due date and APR | A placeholder 0% rate or an invented due date would make payoff and utilization results wrong |
| 2026-09-14 | Aggregator categories map onto one FinPilot vocabulary in `finpilot/integrations/categories.py`; the provider label is kept as `provider_category`, and user corrections win | Card rewards, bills and chat filters need one vocabulary across bank and manual data |
| 2026-09-14 | Only the Plaid adapter exists. MX, Yodlee or another aggregator waits for the owner's `G8.9` decision and would plug into the same apply functions and category table | Nothing here can verify another provider's API, and coverage data should drive the choice |
| 2026-09-14 | CSV import is refused for an account that syncs from a healthy bank connection; CSV and sample workspaces stay for manual accounts and demos | Importing the same activity twice would double count, and removing CSV would strand institutions Plaid does not cover |
| 2026-09-15 | Model wording carries figure tokens; the server inserts exact figures and rejects a typed digit, currency symbol, percent sign or number word | Numeric grounding accepted a real figure attached to the wrong claim; tokens keep every number in code |
| 2026-09-15 | Unrelated requests get a fixed reply before planning, and unrecognized router requests get fixed guidance without model wording | Model calls cost time and could produce unverified off-topic text |
| 2026-09-15 | The answer cache is exact-match per household on verified token wording and validated plans, in process memory for up to 24 hours; similarity caching stays rejected | Figures change with live data, so only wording without figures is safe to reuse |
| 2026-09-15 | Record links, figure sources and confidence that fail `EVIDENCE_SCHEMA` are dropped, not repaired | A wrong link or score is worse than none, and the answer itself is unaffected |
| 2026-09-15 | NeMo Guardrails and Instructor stay out; their "Considered and not adopted" entry and the semantic caching entry now point at `G7.9`, `G7.7` and `G7.10` | The owner asked for these outcomes, and native checks cover every answer path without a new dependency |

## 8. Session log (append newest first)

<!--
Template. Copy it and keep the heading format exactly: date | agent | one-line title.

### YYYY-MM-DD | <agent or person> | <short title>
- **Goal:** what was asked.
- **Changed:** files or areas touched, in one or two lines each.
- **Verified:** commands run and their real results. Say "not run" when not run.
- **Not done / left broken:** anything incomplete, failing, or skipped, and why.
- **Next agent should:** the first concrete thing to do.
-->

### 2026-09-20 | Claude Opus 5 (Claude Code) | Client addresses behind a reverse proxy (G1.2)
- **Goal:** the owner asked to implement `G1.2`, reproducing the problem behind a proxy first.
- **Changed:** new `scripts/verify_proxy_client_addresses.py` and `tests/test_proxy_client_addresses.py`; `finpilot/api/app.py` gained `warn_about_proxy_trust`; `Dockerfile`, `compose.yaml`, `.env.example`, `docs/configuration.md`, `DEPLOYMENT.md`, `CHANGELOG.md`, `tests/test_docs.py` (the setting scan now covers `FORWARDED_ALLOW_IPS`), `ROADMAP.md` and this file. Evidence: `validation/proxy-client-addresses.json`.
- **Verified:** the reproduction passed against an `nginx:alpine` container: untrusted proxy 8x201 then 429 for 9 distinct client addresses, trusted proxy 9x201, trusted proxy with one address 8x201 then 429. `tests/test_proxy_client_addresses.py`, `test_docs.py`, `test_request_logging.py` and `test_hosted.py`: 47 passed. Full suite 841 passed, 3 failed (the memory-file checks, while those files were mid-edit), then 15 of 15 passed. Frontend suites not run.
- **Not done / left broken:** `G1.2.4`, the hosted-ingress confirmation, needs `G4.1`. The Docker image itself was still not built; only the nginx proxy container ran. On Docker Desktop for Windows the container reached the host from `127.0.0.1`, which the uvicorn default already trusts, so the untrusted case had to be forced by naming another address.
- **Next agent should:** ask the owner whether to commit, then take `G5.1` from section 4.

### 2026-09-19 | Claude Opus 5 (Claude Code) | Log Plaid error codes and fix the Sandbox check import
- **Goal:** the owner asked to fix `G8.1.6` and `G8.1.7`, found earlier the same day.
- **Changed:** `finpilot/integrations/plaid.py` (warning log for failed calls with allowlisted identifiers only), `scripts/verify_plaid_sandbox.py` (`create_app` imported inside `run`), `tests/test_plaid_sandbox_check.py` (2 tests), `DEPLOYMENT.md`, `CHANGELOG.md`, `ROADMAP.md`, this file.
- **Verified:** Plaid, bank, aggregation and request-logging test files: 35 passed. Full suite: 831 passed, 3 memory-file tests failed while `HANDOFF.md` and `ROADMAP.md` were mid-edit, then 15 of 15 passed. A live `link/token/create` call to `production.plaid.com` with the owner's Sandbox keys logged `Plaid production /link/token/create failed: status=400 error_type=INVALID_INPUT error_code=INVALID_API_KEYS request_id=...`.
- **Not done / left broken:** the local server on port 8100 still runs code from before these changes. Frontend suites not run.
- **Next agent should:** ask the owner whether to commit, then continue with section 4.

### 2026-09-19 | Claude Opus 5 (Claude Code) | Plaid Link in the browser with Sandbox keys
- **Goal:** the owner asked to run FinPilot with their Plaid keys and connect a bank, first with production keys, then with Sandbox test data.
- **Changed:** `ROADMAP.md` (`G8.1.5` done, new `G8.1.6`, `G8.1.7`, `G8.2.2`, `G8.2` evidence) and this file. Git-ignored only: `.local/plaid-production.env` with the owner's keys and a generated token key, and a launcher `.local/run-plaid-production.py` (`--sandbox`, `--check`). No repository code changed.
- **Verified:** direct `POST /link/token/create` calls: the owner's keys got `INVALID_API_KEYS` from production and succeeded on Sandbox, for both key sets. In Chrome the owner linked Tartan Bank: server log shows `POST /api/bank/link-token` 200, `POST /api/bank/exchange` 200 and `POST /api/bank/sync/...` 200; the database has an active connection with 365 transactions. Launcher `--check` prints the intended database for each mode. Test suites not run, since no code changed.
- **Not done / left broken:** disconnect not exercised. This session's Sandbox user and connection are in `.local/finpilot.db` (see `G8.1.7`). No production link was possible without a production secret.
- **Next agent should:** fix `G8.1.6` and `G8.1.7`.

### 2026-09-15 | Claude Opus 5 (Claude Code) | Figure tokens, answer checks, evidence links and wording cache
- **Goal:** implement the owner's list: deterministic math with the model only wording answers, guardrails against advice hallucinations and off-topic prompts with citations, semantic caching, strict output validation against source records, and confidence and citation scoring with clickable links.
- **Changed:** new `finpilot/ai/facts.py`, `finpilot/ai/evidence.py`, `finpilot/ai/wording_cache.py` and `scripts/measure_figure_tokens.py`. Answer path in `finpilot/ai/graph.py`; advice and topic checks in `finpilot/ai/guardrails.py`; planner cache in `finpilot/ai/workflows.py`; record IDs in `finpilot/ai/tools.py` and `finpilot/engine/ledger.py`; topic gate and evidence in `finpilot/api/assistant_routes.py`; record links and confidence in `finpilot/web/assistant.js` and `assistant.css`; token prompts and an `advise` mode in `finpilot/ai/mock_server.py`. New tests `tests/test_fact_tokens.py`, `tests/test_answer_guards.py`, `tests/test_wording_cache.py` and `tests/test_evidence_links.py`; synthetic drafts in `tests/test_spending_claim_guard.py`, `tests/test_guardrail_numeric_formats.py` and `tests/test_llm_runtime.py` now write figure tokens; `tests/test_capability_workflows.py` validates every answer's evidence fields; `tests/assistant_frontend.mjs` covers the links. Docs: `ARCHITECTURE.md`, `AI_VALIDATION.md`, `README.md`, `CHANGELOG.md`, `AGENTS.md`, `docs/user-guide.md`, `docs/calculation-methods.md` and `finpilot/api/route_docs.py` with regenerated API references. Measurements in `validation/figure-tokens-*.json`. `ROADMAP.md`: `G7.7`, `G7.9`, `G7.10`, `G7.3.3`, `G7.2.4`, an idea and two "Considered and not adopted" entries.
- **Verified:** `pytest -q -p no:cacheprovider` gave 832 passed, 1 warning in 170.31s. All seven frontend suites passed, `tests/frontend.mjs` against a fresh server. Measurements: `llama3.2:latest` at 12 s kept model wording on 5 of 10 questions, 2 rejected (allowance wording 1, typed figure 1), 2 over budget or failed and 1 needed no model call; repeats came from the cache 5 of 5 times with 0 model calls; `llama3.2:latest` at 60 s kept model wording on 6 of 10 questions, 2 rejected (allowance wording 1, typed figure 1), 1 over budget or failed and 1 needed no model call; repeats came from the cache 6 of 6 times with 0 model calls; `llama3.1:8b-instruct-q4_K_M` at 12 s kept model wording on 3 of 10 questions, 0 rejected, 6 over budget or failed and 1 needed no model call; repeats came from the cache 3 of 3 times with 0 model calls; `llama3.1:8b-instruct-q4_K_M` at 60 s kept model wording on 7 of 10 questions, 2 rejected (allowance wording 1, typed figure 1), 0 over budget or failed and 1 needed no model call; repeats came from the cache 7 of 7 times with 0 model calls. A first measurement run was discarded: after one timeout, the client's five-second failure cooldown turned every later question into calculator wording, so the script now turns the cooldown off and records each raw draft. `roadmap.py check` and `check_handoff.py --strict` passed.
- **Not done / left broken:** Nothing committed. Long answers such as the paycheck plan still fall back to calculator wording on these local models. The topic gate and advice check are word lists, and figure roles are checked only for the spending allowance (section 6).
- **Next agent should:** ask the owner whether to commit, then include `scripts/measure_figure_tokens.py` in the `G7.2` host comparison.

### 2026-09-15 | Claude Opus 5 (Claude Code) | Second Plaid Sandbox run and doc reconciliation
- **Goal:** run the Plaid Sandbox check with the keys the owner added.
- **Changed:** rewrote `validation/plaid-sandbox-e2e.json` with a second passing run. Updated `DEPLOYMENT.md`, `README.md`, `CHANGELOG.md`, the `G8.5` evidence line and this file. Kept the `G8.1` status, tasks and handoff facts that Codex recorded. No code changed.
- **Verified:** confirmed both keys were present with format checks that print no values. `.venv/Scripts/python.exe scripts/verify_plaid_sandbox.py` passed at 01:54 UTC on 2026-09-15: register 201, link token 200, Sandbox item created, exchange 200, syncs 200 and 200, disconnect 200, no provider errors. A script compared the evidence with both keys and found neither. `roadmap.py check` and `check_handoff.py --strict` passed.
- **Not done / left broken:** Nothing committed. Hosted staging and production Plaid remain unverified (`G8.1.4`, `G8.2`).
- **Next agent should:** ask the owner whether to commit, then start `G1.2`.

### 2026-09-14 | Codex | Plaid Sandbox credentials installed and verified
- **Goal:** store the owner-provided Plaid Sandbox client ID and secret without committing or printing them, then run the Plaid verifier.
- **Changed:** Updated the git-ignored `.local/plaid-sandbox.env` with non-empty Sandbox values. `validation/plaid-sandbox-e2e.json` now contains a successful Plaid Sandbox evidence run. Updated `ROADMAP.md` to unblock `G8.1`, check `G8.1.1` and `G8.1.2`, and add `G8.1.4` for hosted-staging verification; regenerated `docs/roadmap.html`. Updated this handoff.
- **Verified:** Checked only value lengths for `PLAID_CLIENT_ID`, `PLAID_SECRET` and `PLAID_ENV`. A sandboxed `.venv/Scripts/python.exe scripts/verify_plaid_sandbox.py` run failed with `PROVIDER_UNAVAILABLE`; rerunning the same command with approved network access passed with link token 200, exchange 200, two syncs 200, disconnect 200 and no provider errors. `python scripts/roadmap.py write` could not run because `python` is not on PATH; `.venv/Scripts/python.exe scripts/roadmap.py write` should be used here.
- **Not done / left broken:** No commit. The credentials are local only and must never be printed or committed. The Sandbox journey still needs to be repeated against hosted staging after `G4.1` exists (`G8.1.4`). Full tests were not rerun because only ignored credentials, validation evidence and memory files changed.
- **Next agent should:** use `.venv/Scripts/python.exe` for repository scripts, avoid printing `.local/plaid-sandbox.env`, and start `G1.2` unless the owner asks to commit first.

### 2026-09-14 | Claude Opus 5 (Claude Code) | Plaid Sandbox run waits for keys
- **Goal:** add Plaid Sandbox credentials and run the verification script.
- **Changed:** `scripts/verify_plaid_sandbox.py` gained `--env-file` and `--create-env-file`, per-endpoint Plaid error codes in its evidence, and a clear exit when keys are missing; `tests/test_plaid_sandbox_check.py` covers them. Created the placeholder `.local/plaid-sandbox.env`. Updated `DEPLOYMENT.md`, `AGENTS.md`, `CHANGELOG.md` and the `G8.1` blocker text.
- **Verified:** looked for keys without printing values and found none in the process environment, the Windows user and machine environments, `.env` or `.local/`. Full suite 788 passed, 1 warning in 149.76s. From this machine `sandbox.plaid.com` answered HTTPS with status 200, so a run can reach Plaid once keys exist.
- **Not done / left broken:** The Sandbox run did not happen because no keys exist, so `G8.1` stays blocked. Nothing committed.
- **Next agent should:** once the owner has added keys, run `python scripts/verify_plaid_sandbox.py`.

### 2026-09-14 | Claude Opus 5 (Claude Code) | Automated aggregation through Plaid
- **Goal:** replace manual uploads and sample data with aggregator APIs for balances, transaction categories and liability tracking.
- **Changed:** rewrote `finpilot/integrations/plaid.py` to link credit card and loan accounts, fetch `/liabilities/get`, apply reported terms with `apply_liabilities` and map categories through the new `finpilot/integrations/categories.py`. Added `terms_complete` to `Liability` and `provider_category` to `Transaction`, payoff filters in `finpilot/ai/tools.py`, manual terms completion and the CSV guard in `finpilot/services/workspace.py`, liabilities wiring in `finpilot/api/bank_routes.py` and `finpilot/services/bank_operations.py`, and UI notes in `connect.js`, `pages.js` and `accounts.js`. Added `scripts/verify_plaid_sandbox.py` and three test files, updated `tests/test_bank_linking.py`, and documented the change in `ARCHITECTURE.md`, `DEPLOYMENT.md`, `README.md`, `AGENTS.md`, `CHANGELOG.md` and three files in `docs/`. Roadmap: `G8.5.1`, `G8.7.5` and `G8.7.6` done, `G8.1.3` added and done, `G8.1` blocked on credentials.
- **Verified:** full suite 785 passed, 1 warning in 146.47 s. All seven frontend suites passed; `tests/frontend.mjs` ran against a fresh server on a free port. Plaid's Liabilities fields, link token options and category taxonomy were read from Plaid's published documentation on 2026-09-14. No live Plaid call was made.
- **Not done / left broken:** Nothing committed. No Sandbox or production Plaid call was made, so `optional_products`, Liabilities responses and category labels are unverified against Plaid itself (`G8.1`). Plaid Link still requires Transactions, so liabilities-only institutions do not appear. No MX or Yodlee adapter (`G8.9`). Investments are not linked (`G8.5.2`), and webhooks, reconnect and scheduled sync remain (`G8.3`, `G8.4`, `G8.6`).
- **Next agent should:** ask the owner for Plaid Sandbox credentials and run `python scripts/verify_plaid_sandbox.py`, then start `G1.2`.

### 2026-09-14 | Claude Opus 5 (Claude Code) | Private request logging
- **Goal:** start and finish `G1.1`, privacy-safe request logging.
- **Changed:** added `finpilot/api/request_logging.py`, called from `create_app`, and `--no-access-log` in the `Dockerfile`. Moved `/api/debt/compare`, `/api/debt/what-if`, `/api/mortgage/scenarios`, `/api/mortgage/biweekly`, `/api/cards/utilization`, `/api/tax/net-benefit` and `/api/documents/search` to `POST` JSON bodies, and replaced `GET /api/transactions` with `POST /api/transactions/search`. Updated `finpilot/web/app.js`, `finpilot/web/manage.js`, `finpilot/api/route_docs.py`, the generated API references and six test files. Added `tests/test_request_logging.py`, a "Request logs" section in `DEPLOYMENT.md`, and the rule in `ARCHITECTURE.md`, `AGENTS.md` and `CHANGELOG.md`. Roadmap: `G1.1` done, `G4.1.5` added.
- **Verified:** full suite 775 passed, 1 warning, in 139.21 s and again in 145.15 s; each new server test takes about 1.6 s. The new server tests started the container's Uvicorn command and plain Uvicorn: both printed request lines, the container command printed no access lines, and neither printed the search term, the amount or a query string. All seven frontend suites passed; `tests/frontend.mjs` ran against a fresh server on a free port whose log held 6 request lines and no query strings. `scripts/build_docs.py --check` and `tests/test_docs.py` passed.
- **Not done / left broken:** Nothing committed. No Docker image or real proxy was run, so the nginx format in `DEPLOYMENT.md` is unverified; `G4.1.5` tracks the real ingress. The full suite took about 140 s against 80 s earlier the same day. Turning the request logger off did not help: 100 request-heavy tests took 65.31 s with it off and 24.28 s with it on, so timings here swing with machine load.
- **Next agent should:** ask the owner about committing, then start `G1.2`.

### 2026-09-14 | Claude Opus 5 (Claude Code) | Live roadmap page for the owner
- **Goal:** keep the owner's private roadmap page current every time the shared memory changes, and show the goals being worked on.
- **Changed:** added `scripts/roadmap_artifact.py` (build, status, mark-published, stop-hook) and `tests/test_roadmap_artifact.py`. Added a Stop hook in the untracked `.claude/settings.local.json` and ignored that file in `.gitignore`. Recorded the page in `AGENTS.md`, `CHANGELOG.md` and this file, and added an idea to `ROADMAP.md` to ship the builder in the kit. Committed and pushed at the owner's request.
- **Verified:** full suite 763 passed, 1 warning in 80.57 s. The hook command as saved printed a block decision while the page was unpublished and printed nothing when `stop_hook_active` was true. `roadmap.py check` and `check_handoff.py --strict` passed. One headless Chrome screenshot of the built page showed the new panel; the page was then republished and marked published.
- **Not done / left broken:** The hook was not exercised end to end, because Claude Code loads a new hook file only after `/hooks` or a restart. The builder is not in the portable kit yet.
- **Next agent should:** run `python scripts/roadmap_artifact.py status`, then start `G1.1`.

### 2026-09-14 | Claude Opus 5 (Claude Code) | Filled the documentation gaps
- **Goal:** Write the missing documentation found by the audit, after the owner agreed.
- **Changed:** Added `docs/user-guide.md`, `docs/calculation-methods.md`, `docs/configuration.md`, `docs/frontend.md`, `SECURITY.md`, `CONTRIBUTING.md`, `CHANGELOG.md`, `finpilot/api/route_docs.py`, `scripts/build_docs.py`, `tests/test_docs.py` and the generated `docs/api-reference.md`, `docs/api/openapi.json`, `docs/ai-capabilities.md` and `docs/data-model.md`. Changed `finpilot/api/app.py` to apply the route documentation, plus `README.md`, `AGENTS.md` facts, `DEPLOYMENT.md`, `.env.example` and the roadmap. Committed and pushed at the owner's request.
- **Verified:** All 84 operations in the OpenAPI schema have a summary and a description, and `scripts/build_docs.py --check` passes. The capability reference lists 80 capabilities, each with an example request; the data model lists 20 tables. Relative links in the new documents resolve. The full suite gave 761 passed, 1 warning in 79.23 s, and the memory and documentation tests gave 23 passed. The calculation methods were written from the engine source and its docstring worked examples, not re-derived numerically. A grep showed that only `tests/frontend.mjs` references a running server.
- **Not done / left broken:** The app does not link to the user guide (`G13.10`). There is no license (`G13.2`), no enabled private vulnerability reporting or promised response time (`G13.3`), and no specification for the requirement IDs (`G13.5`). Issue templates and `CODEOWNERS` wait on whether outside contributions are expected (`G13.11.2`). Frontend suites were not rerun.
- **Next agent should:** Ask the owner about the open documentation decisions, then start `G1.1`.

### 2026-09-14 | Claude Opus 5 (Claude Code) | Documentation audit
- **Goal:** Answer whether the repository has complete feature, reference and technical documentation.
- **Changed:** Added roadmap goal `G13. Documentation` with 12 sub-goals and task `G6.2.2`; regenerated `docs/roadmap.html`; updated sections 1, 2, 4 and 6 here. No application code or other documents.
- **Verified:** Listed every tracked document and its headings. Compared every setting read in `finpilot/` and `scripts/` with `.env.example`, `DEPLOYMENT.md`, `README.md` and `MCP.md`. Parsed `finpilot/api/` with `ast`: none of the route handlers has a docstring, and 62 of 69 modules have module docstrings. Counted 18 tables in migrations, 12 named in `ARCHITECTURE.md`. Found spec-section and requirement-ID citations in code with no specification document tracked. Checked the README verification commands against `tests/*_frontend.mjs`. Confirmed `LICENSE`, `SECURITY.md`, `CONTRIBUTING.md`, `CHANGELOG.md`, issue templates and `CODEOWNERS` are absent.
- **Not done / left broken:** No documentation was written; the owner asked a question. Nothing committed.
- **Next agent should:** Ask the owner about committing this audit and the `G13.2` and `G13.5` decisions.

### 2026-09-14 | Claude Opus 5 (Claude Code) | Committed the shared pages and enabled local enforcement
- **Goal:** Commit the roadmap page and the kit guide page, and finish implementing the agent memory kit in this repository.
- **Changed:** Added `scripts/roadmap_page.html`, page generation in `scripts/roadmap.py`, `docs/roadmap.html`, `docs/agent-memory-kit/guide_page.html`, `docs/agent-memory-kit/build_guide.py`, `docs/agent-memory-kit/guide.html` and `.githooks/pre-push`. Updated `.agent-memory.json`, `tests/test_roadmap.py`, `tests/test_agent_memory_kit.py`, the kit templates and guide, `AGENTS.md` facts and `G5.8`. Set `core.hooksPath` in this clone. No application code.
- **Verified:** `gh auth status` still lists only `gist`, `read:org` and `repo`. Both pages build with no leftover placeholders, and `roadmap.py check` failed on the missing page before `write` and passed after. The pre-push hook passed when run by hand. The memory tests gave 20 passed with `-p no:cacheprovider --basetemp <scratchpad>`, including new page tests. Full collection is 758 tests. Full suite not run because no application code changed.
- **Not done / left broken:** The GitHub workflow is still inactive (`G5.8.2`). Pages were not visually re-checked in a browser after moving into the repository; their design is unchanged from the published versions.
- **Next agent should:** Ask the owner to grant the `workflow` scope, then complete `G5.8.2`.

### 2026-09-13 | Claude Opus 5 (Claude Code) | Pushed the shared memory and prepared CI enforcement
- **Goal:** Confirm the roadmap page is fully captured in shared memory, then commit, push and activate the memory system.
- **Changed:** Committed `7389d2f` and pushed `main`. Added `--base` to `scripts/check_handoff.py`, a branch-check test in `tests/test_handoff.py`, the workflow template `docs/agent-memory-kit/templates/.github/workflows/agent-memory.yml`, kit guide updates and roadmap sub-goal `G5.8`, and closed `G6.1`. No application code.
- **Verified:** The published roadmap page data equals `scripts/roadmap.py json` output apart from line numbers (12 goals, 74 sub-goals, 171 tasks). `git ls-remote origin refs/heads/main` returned `7389d2f` after the push. No secret-shaped strings were found in the staged diff. The memory tests gave 17 passed with `-p no:cacheprovider --basetemp <scratchpad>`, including the new branch check. `check_handoff.py --base 3a2bdc9` passed. Full collection is 755 tests. Full suite not run because no application code changed.
- **Not done / left broken:** The workflow is not active in `.github/workflows/` because the token lacks the `workflow` scope. `prototype/` remains undecided.
- **Next agent should:** Ask the owner to grant the scope, then complete `G5.8.2`.

### 2026-09-13 | Claude Opus 5 (Claude Code) | Installed the agent memory kit here word for word
- **Goal:** Implement the agent memory kit in this repository exactly as it is meant to be installed elsewhere.
- **Changed:** Aligned `AGENTS.md`, the `ROADMAP.md` rules header and intro command, `CLAUDE.md` and this file's commands with the kit templates. Extended `tests/test_agent_memory_kit.py` with protocol, roadmap-rules and `CLAUDE.md` checks. No application code.
- **Verified:** Compared each live file with its template first: the differences were wording and interpreter paths, while the scripts, their tests and the PR template were already identical. `python --version` reports Python 3.12.10 on PATH. After aligning, `python scripts/roadmap.py check` and `python scripts/check_handoff.py --strict` pass, the three memory test files gave 16 passed with `-p no:cacheprovider --basetemp <scratchpad>`, and full collection is 754 tests. Full suite not run because no application code changed.
- **Not done / left broken:** Nothing committed or pushed. Running the validators in continuous integration is still open as `G5.1`.
- **Next agent should:** Ask the user about committing this work, then start `G1.1`.

### 2026-09-13 | Claude Opus 5 (Claude Code) | Portable agent memory kit for other repositories
- **Goal:** Explain how the shared memory is designed and make it reusable, unchanged, in other repositories.
- **Changed:** Added `docs/agent-memory-kit/` (guide and templates), `.agent-memory.json`, root `conftest.py` and `tests/test_agent_memory_kit.py`. Updated `scripts/check_handoff.py` (config-driven code paths, cache paths ignored, no bytecode when loading the roadmap tool) and `AGENTS.md` (accurate tool list, kit in the documentation map). No application code.
- **Verified:** Installed the templates into a fresh git repository in the scratchpad. The strict check passed on a clean install, pytest there gave 12 passed, the strict check still passed with only `__pycache__` folders untracked, changing `src/app.py` without touching `HANDOFF.md` failed with exactly one problem, and updating `HANDOFF.md` made it pass. An earlier run of the same scenario counted `scripts/__pycache__` as code, which led to the cache fix. In FinPilot, `check_handoff.py --strict` passes, the three memory test files gave 14 passed with `-p no:cacheprovider --basetemp <scratchpad>`, and full collection is 752 tests with `docs/` excluded. Full suite not run because no application code changed.
- **Not done / left broken:** Nothing committed. The kit has not yet been installed in a real second repository.
- **Next agent should:** Ask the user about committing this work, then start `G1.1`.

### 2026-09-13 | Claude Opus 5 (Claude Code) | Roadmap with long-term goals, sub-goals and tasks
- **Goal:** Add long-term goals with sub-goals and tasks, classified into short-term, mid-term and long-term, covering the earlier gap review and the production-readiness list the user pasted.
- **Changed:** Added `ROADMAP.md`, `scripts/roadmap.py` and `tests/test_roadmap.py`. Updated `scripts/check_handoff.py` so it validates the roadmap and the roadmap IDs in this file, plus `AGENTS.md`, `CLAUDE.md`, `.github/PULL_REQUEST_TEMPLATE.md` and sections 1 to 8 here. No application code.
- **Verified:** Checked the pasted suggestions against the code before classifying them: Fernet credential encryption exists; input safety rejects credentials but masks no account numbers or SSNs; LangSmith tracing is opt-in without redaction; audit rows are insert-only in code but not enforced by the database; CSV import detects duplicates and defaults categories to `uncategorized`; `engine/recurring.py` tracks rule occurrences, not subscriptions; document extraction runs inside the upload request; `engine/ledger.py` is a forecast ledger, not double-entry; no pytest markers exist. `scripts/roadmap.py check` passes. `pytest tests/test_roadmap.py tests/test_handoff.py -p no:cacheprovider --basetemp <scratchpad>` gave 12 passed; without `--basetemp`, two tests errored on a sandbox `PermissionError`. A copy of this file with a closed ID and an unknown ID in section 4 failed validation as intended. Full suite not run because no application code changed.
- **Not done / left broken:** Nothing committed. Thirteen roadmap sub-goals wait on user decisions. Horizons and priorities are proposals for the user to adjust.
- **Next agent should:** Ask the user about committing this work and the short-term decisions, then start `G1.1`.

### 2026-09-13 | Claude Opus 5 (Claude Code) | Product-readiness gap review
- **Goal:** Answer what is missing to make FinPilot a complete, launchable product.
- **Changed:** `HANDOFF.md` only: sections 1, 2, 4 and 6 plus this entry. No application code.
- **Verified:** Read the auth service, app middleware, all routes, the Plaid adapter, domain models, `Dockerfile`, `compose.yaml` and `DEPLOYMENT.md`. Searched for schedulers, email, MFA, account deletion, export, metrics, error tracking, billing, legal text, budgets, OCR, admin views and PWA assets; none exist. Ran the app with uvicorn 0.52.4 defaults on port 8765 against a scratchpad SQLite database: `/api/health` returned 200, zero `finpilot.requests` lines were printed, and the access log printed `GET /api/transactions?q=therapy-copay&category=medical`. Read `uvicorn.config.Config.__init__`: `FORWARDED_ALLOW_IPS` defaults to `127.0.0.1`. Test suite not run because no code changed.
- **Not done / left broken:** No fixes applied, since the user asked a question. The proxy finding comes from uvicorn defaults plus missing configuration, not from a run behind a real proxy. Notes about privacy law are general, not legal advice.
- **Next agent should:** Confirm priorities with the user, then fix request logging and forwarded-IP trust first.

### 2026-09-13 | Claude Sonnet 5 (Claude Code) | Committed the accumulated work and the handoff system
- **Goal:** Commit the repository's pending work at the user's request ("commit").
- **Changed:** Staged and committed everything except `prototype/` (28 modified files, about 90 new files including the handoff system from the previous entry). Commit `76f95b1` on `main`. Updated this file's sections 1, 2, 4 and 7 to reflect the commit.
- **Verified:** Reviewed the staged `.env.example` diff by hand (only adds commented `FINPILOT_MCP_SERVERS` guidance, no real values). Scanned the full staged diff for secret-shaped strings; the only hit was a test fixture (`test_credentials_rejected_before_storage_or_model`) asserting that credential-injection attempts get HTTP 422, not a real credential. `git status --short` after commit shows only `?? prototype/`. `scripts/check_handoff.py --strict` passes on the updated file.
- **Not done / left broken:** `main` was not pushed to `origin`. `prototype/` remains untracked, its own nested git repo, undecided. Test suite was not re-run after the commit since the commit only staged already-written, already-tested files; no code was newly authored in this session.
- **Next agent should:** Confirm with the user whether to push `main`, then resolve `prototype/` per the P0 item in section 4.
